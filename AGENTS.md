# AGENTS.md

## 프로젝트 개요

이 프로젝트는 C# 기반의 MES(Manufacturing Execution System) 프로그램이다.

## 기술 스택

- C#
- .NET
- Windows Forms
- DevExpress
- FarPoint Spread
- Oracle Database
- PLC 통신
- Mitsubishi PLC

## 프로젝트 구조

- `MES.Core`: 공통 로직 및 핵심 기능
- `MES.Data`: 데이터베이스 접근 및 SQL
- `MES.UI`: WinForms 화면
- `MES.PLC`: PLC 통신 관련 기능

## 코드 작성 규칙

- 기존 프로젝트의 코딩 스타일을 우선적으로 따른다.
- 기존 메서드와 클래스의 역할을 먼저 확인한 후 수정한다.
- 불필요하게 기존 구조를 크게 변경하지 않는다.
- 변수명은 기존 프로젝트의 명명 규칙을 따른다.
- DB 연결 및 PLC 연결 로직은 임의로 변경하지 않는다.

## 데이터베이스

- Oracle Database를 사용한다.
- 기존 SQL 스타일과 테이블 구조를 우선적으로 확인한다.
- 테이블 구조를 변경하기 전에 반드시 기존 코드와 사용처를 확인한다.
- SQL 변경 시 기존 호출부에 미치는 영향을 확인한다.

## PLC

- PLC 주소와 데이터 타입을 임의로 변경하지 않는다.
- PLC 관련 코드를 수정할 때 기존 주소 매핑과 호출 흐름을 확인한다.
- 통신 오류 발생 시 기존 예외 처리 및 로그 방식을 유지한다.

## 수정 원칙

코드를 수정하기 전에:

1. 관련 파일을 먼저 확인한다.
2. 해당 메서드가 어디서 호출되는지 확인한다.
3. 관련 DB 테이블과 SQL을 확인한다.
4. 수정 범위를 최소화한다.
5. 수정 후 빌드 오류가 없는지 확인한다.

## 원격 변경 작업

- `git push`, 원격 브랜치 생성·삭제, Pull Request 생성 등 실제 원격 상태를 변경하는 작업은 실행 전에 반드시 사용자에게 확인받는다.

## 금지 사항

- 사용자 요청 없이 대규모 리팩터링을 하지 않는다.
- 기존 기능을 삭제하지 않는다.
- DB 테이블을 임의로 변경하지 않는다.
- PLC 주소를 임의로 변경하지 않는다.
- 비밀번호, DB 계정, API 키를 코드에 하드코딩하지 않는다.
